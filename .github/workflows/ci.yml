name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  TURBO_REMOTE_CACHE_READY: "false"

jobs:
  build:
    name: Build & Type Check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Show node and pnpm versions
        run: node --version && pnpm --version

      - run: pnpm install --frozen-lockfile

      - run: pnpm run build

      - run: pnpm run build --filter @openclaw/suite-integration-tests

  lint:
    name: Lint
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - name: Show node and pnpm versions
        run: node --version && pnpm --version

      - run: pnpm install --frozen-lockfile
      - run: pnpm run lint || true

  test:
    name: Integration Tests
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - run: pnpm install --frozen-lockfile

      - name: Run standalone integration test
        run: npx tsx tests/integration/full-scenario.test.ts

  coverage:
    name: Test Coverage
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v5
        with:
          node-version: 22

      - name: Install pnpm
        uses: pnpm/action-setup@v6

      - run: pnpm install --frozen-lockfile
      - run: pnpm --filter @openclaw/suite-integration-tests run test:coverage || true

      - name: Upload coverage to Codecov
        if: always()
        uses: codecov/codecov-action@v4
        with:
          files: ./coverage/lcov.info
          fail_ci_if_error: false
